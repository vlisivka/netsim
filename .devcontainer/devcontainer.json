{
  "image": "mcr.microsoft.com/devcontainers/php",
  "features": {
  },
  "postCreateCommand": 
    "sudo rm -rf /var/www/html && sudo ln -s /workspaces/netsim/ /var/www/html && sudo mkdir /var/www-data && sudo chown www-data:www-data /var/www-data && sudo apt update && sudo apt install sqlite3 && sudo bash /workspaces/netsim/createDB.bash",
  "postAttachCommand": {
    "server": "sudo apache2ctl start"
  },
  "portsAttributes": {
    "80": {
      "label": "NetSim",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "codespaces": {
      "openFiles": ["README.md"]
    }
  }
}
